# .env template file
# Instructions:
# - Duplicate this file and rename it to '.env'
# - Fill in the required env variables and secrets below
# - Run the application with 'yarn dev'

NEXT_PUBLIC_GRAPHQL_URL=http://localhost:3000
ZONE_BFF_SERVER_URL=http://localhost:4000
ENVIRONMENT=dev
